[tool.poetry]
name = "fakenews detectar"
version = "0.1.0"
description = "project"
authors = ["y"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
scikit-learn = "^1.5.1"
matplotlib = "3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
